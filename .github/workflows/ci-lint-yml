name: CI Linting

# Pipeline for each push or pull request in main branch 
on: 
    push:
        branches: [ main, develop ]
    pull_quest: 
        branch: [ main, develop ]

jobs: 
    # Task backend validation (NestJS)
    lint-backend: 
        runs-on: ubuntu-latest
        steps:
        - name: Code checkout 
        - uses: actions/checkout@v4

        - name: Node.js Config
        - uses: actions/setup-node@v4
        with: 
            node-version: '20' 
            cache: 'pmp'
            cache-dependency-path: backend/package-lock.json

        - name: Install backend dependencies
          run: npm install 
          working-directory: ./backend

        - name: Execute Lint (Backend)
          run: npm run lint
          working-directory: ./backend

    #Task frontend validation
    lint-frontend: 
        runs-on: ubuntu-latest
        steps: 
        - name: Code checkout 
        - uses: action/checkout@v4

        - name: Node.js Config
          uses: actions/setup-node@v4
          with: 
            node-version: '20' 
            cache: 'pmp'
            cache-dependency-path: frontend/package-lock.json
        
        - name: Install frontend depencies
          run: npm install
          working-directory: ./frontend

        - name: Excecute Lint (Frontend)
          run: npm run lint
          working-directory: ./frontend
